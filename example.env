########################################
# Image Configuration
# The image is pre-built and pushed to a registry
########################################

# Container Registry (usually ghcr.io for GitHub Container Registry)
IMAGE_REGISTRY=ghcr.io/rydizz214/uhf-server-docker

# Image Tag (version)
IMAGE_TAG=1.5.1

# Pull policy: always, missing, or never
# - missing: only pull if not present locally (recommended)
# - always: always pull latest from registry
# - never: use local image only
PULL_POLICY=missing

########################################
# Container Configuration
########################################

# UHF Server port inside container (usually 8000)
UHF_PORT=8000

# Host port to map to UHF Server
# Similar to snappier-server: host 7429 -> container 8000
# UHF Server: host 8002 -> container 8000
HOST_PORT=8002

# Host directory for recordings (bind-mount to container:/recordings)
HOST_RECORDINGS_DIR=/data/recordings/uhf-server

# Host directory for UHF database or other server data
HOST_UHF_DATA_DIR=/data/recordings/uhf-server

########################################
# UHF Server Options
########################################

# Enable commercial detection (true/false or 1/0)
UHF_ENABLE_COMMERCIAL_DETECTION=true

# Comskip additional arguments
# Common options: --ini, --detectfiller, --standalonecmd
COMSKIP_ARGS=--ini

# Timezone for the container
TZ=America/New_York

########################################
# Advanced Options (usually not needed)
########################################

# Password (optional - uncomment to enable)
# UHF_PASSWORD=mysupersecretpassword
